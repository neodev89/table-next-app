"use client";

import { navbarArray } from "../typesComponent/navbarTypes/navbarTypes";

const Navbar = ({
    navArray
}: navbarArray) => {
    return (
        <nav>
            
        </nav>
    )
}